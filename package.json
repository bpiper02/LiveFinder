{
  "name": "livefinder",
  "private": true,
  "version": "0.3.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "qa": "node --check app.js && node --check extension/background.js && node --check extension/bridge.js && node --check extension/nero.js"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
